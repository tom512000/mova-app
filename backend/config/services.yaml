# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    app.tmdb.api_key: '%env(TMDB_API_KEY)%'
    app.tmdb.api_base_url: '%env(TMDB_API_BASE_URL)%'
    app.tmdb.image_base_url: '%env(TMDB_IMAGE_BASE_URL)%'
    # No Letterboxd parameter here. Which account a feed is read from, and whether it is
    # read on a schedule, belong to each User entity row and are set from the Import screen:
    # one installation-wide setting cannot describe two people syncing two different feeds.

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        # Service-specific env values (TMDB key, Letterboxd username, ...) are injected via
        # #[Autowire(param: '...')] attributes on the individual service constructors instead
        # of a global bind here, since Symfony errors on bindings no service ends up using.

    # Every class implementing ImporterInterface is auto-tagged so ImporterRegistry can
    # collect them via #[TaggedIterator] — new Letterboxd file types only need a new class.
    _instanceof:
        App\Service\Import\ImporterInterface:
            tags: ['app.importer']

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    # $debug isn't autowirable (it's a container parameter, not a typed service).
    App\EventSubscriber\ApiExceptionSubscriber:
        arguments:
            $debug: '%kernel.debug%'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

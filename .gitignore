# Root env (real secrets) — .env.example and .env.prod.example are the committed templates
/.env
/.env.prod

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Letterboxd exports you might drop here for local testing — never commit personal data
/exports/
*.zip
